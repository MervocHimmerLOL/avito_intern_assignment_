## Позитивные сценарии

### 1. Создание объявления `POST /api/1/item`

| TC-ID | Описание | Предусловия | Шаги | Ожидаемый результат |
|-------|----------|-------------|------|-------------------|
| **TC-001** | Успешное создание объявления с валидными данными | - | 1. Сформировать и отправить `POST`-запрос на `/api/1/item` с корректным JSON-телом.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `200 OK`<br>Тело: `{"status": "Сохранили объявление - <uuid>" }`<br>Объявление сохранено (см. POS-10) |
| **TC-003** | Создание с граничными допустимыми значениями | - | 1. Сформировать и отправить `POST`-запрос на `/api/1/item`, указав: `price: 0`, `name`: 255 символов, `sellerID`: мин./макс. допустимое целое число.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить сохранённые данные на отсутствие искажений. | `200 OK`<br>Данные сохранены без искажений |
| **TC-004** | Множественное создание для одного продавца | Существует валидный `sellerID` в диапазоне 111111–999999 | 1. Последовательно отправить три `POST`-запроса на `/api/1/item` с одинаковым `sellerID`.<br>2. Для каждого запроса проверить HTTP-статус.<br>3. Сравнить полученные `id` и привязку к продавцу. | Все 3 объявления созданы с уникальными `id`, корректная связь с продавцом подтверждена |

### 2. Получение объявления по ID `GET /api/1/item/:id`

| TC-ID | Описание | Предусловия | Шаги | Ожидаемый результат |
|-------|----------|-------------|------|-------------------|
| **TC-005** | Успешное получение существующего объявления | Создано объявление (см. `TC-001`) | 1. Сформировать и отправить `GET`-запрос на `/api/1/item/{id_созданного_объявления}`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `200 OK`<br>Тело содержит JSON типа:<br>`{"id": "string", "sellerId": "integer", "name": "string", "price": "integer", "statistics": {"likes": "integer", "viewCount": "integer", "contacts": "integer"}, "createdAt": "string"}` |

### 3. Получение всех объявлений продавца `GET /api/1/:sellerID/item`

| TC-ID | Описание | Предусловия | Шаги | Ожидаемый результат |
|-------|----------|-------------|------|-------------------|
| **TC-006** | Получение списка объявлений продавца | Создано 3 объявления (см. `TC-001`/`TC-004`) | 1. Сформировать и отправить `GET`-запрос на `/api/1/{sellerID}/item`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить структуру и количество элементов в массиве. | `200 OK`<br>Массив из 3 объектов с корректными данными |
| **TC-007** | Получение пустого списка | Создан/используется `sellerID` без привязанных объявлений | 1. Сформировать и отправить `GET`-запрос на `/api/1/{sellerID}/item`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `200 OK`<br>Пустой массив: `[]` |

### 4. Получение статистики `GET /api/1/statistic/:id`

| TC-ID | Описание | Предусловия | Шаги | Ожидаемый результат |
|-------|----------|-------------|------|-------------------|
| **TC-008** | Успешное получение статистики | Создано объявление (см. `TC-001`) | 1. Сформировать и отправить `GET`-запрос на `/api/1/statistic/{id_объявления}`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `200 OK`<br>Массив/объект с полями `{"likes": int, "viewCount": int, "contacts": int}`, значения соответствуют заданным при создании |

### 5. Получение статистики `GET /api/2/statistic/:id`

| TC-ID | Описание | Предусловия | Шаги | Ожидаемый результат | 
|-------|----------|-------------|------|-------------------|
| **TC-009** | Успешное получение статистики | Создано объявление (см. `TC-001`) | 1. Сформировать и отправить `GET`-запрос на `/api/2/statistic/{id_объявления}`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `200 OK`<br>Массив/объект с полями `{"likes": int, "viewCount": int, "contacts": int}`, значения соответствуют заданным при создании |

---

## Негативные сценарии

### 1. Создание объявления `POST /api/1/item`

| TC-ID | Описание | Шаги | Ожидаемый результат |
|-------|----------|------|-------------------|
| **TC-010** | Пустое тело запроса | 1. Сформировать и отправить `POST`-запрос на `/api/1/item` с пустым телом `{}`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `400 Bad Request`<br>Тело: `{"result": {"message": "поле name обязательно", "messages": {}}, "status": "400"}` |
| **TC-011** | Отсутствие `sellerID` | 1. Сформировать и отправить `POST`-запрос на `/api/1/item` с телом, в котором отсутствует поле `sellerID`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `400 Bad Request`<br>Тело: `{"result": {"message": "", "messages": {}}, "status": "не передано тело объявления"}` |
| **TC-012** | Некорректный тип: `price` как строка | 1. Сформировать и отправить `POST`-запрос на `/api/1/item`, указав `"price": "1000"`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `400 Bad Request`<br>Тело: `{"result": {"message": "", "messages": {}}, "status": "не передано тело объявления"}` |
| **TC-013** | Отрицательная цена | 1. Сформировать и отправить `POST`-запрос на `/api/1/item`, указав `"price": -100`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `400 Bad Request`<br>Тело: `{"result": {"message": "", "messages": {}}, "status": "не передано тело объявления"}` |
| **TC-014** | Отрицательные значения в `statistics` | 1. Сформировать и отправить `POST`-запрос на `/api/1/item`, указав отрицательные значения для `likes`, `viewCount`, `contacts`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `400 Bad Request`<br>Тело: `{"result": {"message": "", "messages": {}}, "status": "не передано тело объявления"}` |

### 2. Получение объявления по ID `GET /api/1/item/:id`

| TC-ID | Описание | Шаги | Ожидаемый результат |
|-------|----------|------|-------------------|
| **TC-015** | Несуществующий ID | 1. Сформировать и отправить `GET`-запрос на `/api/1/item/{валидный_но_несуществующий_uuid}`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `404 Not Found`<br>Тело: `{"result": {"message": "item fd9053e5-0966-4727-972d-aa82bc797525 not found", "messages": null}, "status": "404"}` |
| **TC-016** | Неверный формат ID | 1. Сформировать и отправить `GET`-запрос на `/api/1/item/1` (некорректный UUID).<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `400 Bad Request`<br>Тело: `{"result": {"message": "ID айтема не UUID: 1", "messages": {}}, "status": "400"}` |

### 3. Получение объявлений по sellerID `GET /api/1/:sellerID/item`

| TC-ID | Описание | Шаги | Ожидаемый результат |
|-------|----------|------|-------------------|
| **TC-017** | Несуществующий sellerID | 1. Сформировать и отправить `GET`-запрос на `/api/1/999999/item`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `200 OK` с `[]` |
| **TC-018** | Неверный тип sellerID | 1. Сформировать и отправить `GET`-запрос на `/api/1/abc/item`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `400 Bad Request`<br>`{"result": {"message": "передан некорректный идентификатор продавца", "messages": {}}, "status": "400"}` |
| **TC-019** | Отрицательный sellerID | 1. Сформировать и отправить `GET`-запрос на `/api/1/-5/item`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `400 Bad Request` |

### 4. Получение статистики `GET /api/1/statistic/:id`

| TC-ID | Описание | Шаги | Ожидаемый результат |
|-------|----------|------|-------------------|
| **TC-020** | Статистика для несуществующего id | 1. Сформировать и отправить `GET`-запрос на `/api/1/statistic/{валидный_но_несуществующий_uuid}`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `404 Not Found`<br>`{"result": {"message": "statistic fd9053e5-0966-4727-972d-aa82bc797525 not found", "messages": null}, "status": "404"}` |
| **TC-021** | Статистика для некорректного id | 1. Сформировать и отправить `GET`-запрос на `/api/1/statistic/{некорректный_id}`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `400 Bad Request`<br>`{"result": {"message": "передан некорректный идентификатор объявления", "messages": {}}, "status": "400"}` |

### 5. Получение статистики `GET /api/2/statistic/:id`

| TC-ID | Описание | Шаги | Ожидаемый результат |
|-------|----------|------|-------------------|
| **TC-022** | Статистика для несуществующего id | 1. Сформировать и отправить `GET`-запрос на `/api/2/statistic/{валидный_но_несуществующий_uuid}`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `404 Not Found`<br>`{"result": {"message": "statistic ad6863e5-0556-3322-972d-aa82bc797525 not found", "messages": null}, "status": "404"}` |
| **TC-023** | Статистика для некорректного id | 1. Сформировать и отправить `GET`-запрос на `/api/2/statistic/{некорректный_id}`.<br>2. Проверить HTTP-статус ответа.<br>3. Проверить тело ответа. | `400 Bad Request`<br>`{"result": {"message": "передан некорректный идентификатор объявления", "messages": {}}, "status": "400"}` |
